func factorial(n: int) : int {
  if n <= 1 {
    return 1;
  }

  var result: int;
  result = 1;

  var i: int;
  i = 2;
  while i < n {
    result = result * i;
    i = i + 1;
  }
  return result;
}

func main(): int {
  var r: int;
  r = factorial(10);
  printf("10! = %lld\n", r);
  return 0;
}
