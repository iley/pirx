func random_number(): int {
  return 42;
}

func add(x: int, y: int): int {
  return x + y;
}

func main(): int {
	var x: int;
	x = "foo";
  2 + "foo";

  var s : string;
  s = x;
  s = random_number();

  add(1, "foo");
  add("foo", 2);

  !"foo";

	return 0;
}
