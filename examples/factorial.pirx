func factorial(n: int) : int {
  if n <= 1 {
    return 1;
  }

  var result = 1;
  var i = 2;
  for var i = 2; i < n; i++ {
    result = result * i;
  }
  return result;
}

func main(): int {
  printf("10! = %lld\n", factorial(10));
  return 0;
}
