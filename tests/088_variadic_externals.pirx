// Check that the external function calling convention is implemented correctly.
// On Linux the first 8 arguments of variadics go into X0-X7, the rest goes onto stack.
// On MacOS all variadic args go onto stack.
func main(): int {
  printf("%d %d %d %d %d %d %d %d\n", 1, 2, 3, 4, 5, 6, 7, 8);
  return 0;
}
