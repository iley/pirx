func factorial(n: int) : int {
  var result = 1;
  for var i = 2; i < n; i++ {
    result = result * i;
  }
  return result;
}

func main(): int {
  printf("10! = %lld\n", factorial(10));
  return 0;
}
